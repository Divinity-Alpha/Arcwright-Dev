# Python Dependencies

The Arcwright MCP server (Scripts/mcp_server/server.py) uses only
Python standard library modules (json, socket, subprocess, os, sys,
threading, logging). No third-party Python packages are bundled here.

Python 3.9+ must be installed separately by the user.
MCP mode requires Python. TCP mode works without Python.
